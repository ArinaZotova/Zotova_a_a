#include<iostream>
int main()
{
	int a, d1, d2, i, k;
	std::cin >> a;
	k = 0;
	for (i = 2; i < a; i++)
	{
		d1 = i;
		d2 = a - i;
		if (d1 % 2 == 0 && d2 % 2 == 0)
		{
			k += 1;
		}
	}
	if (k > 0)
	{
		std::cout << "YES";
	}
	else
	{
		std::cout << "NO";
	}
}
